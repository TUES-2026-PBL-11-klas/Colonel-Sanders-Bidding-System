# Database Configuration
# For local development (connects to localhost)
DB_URL=jdbc:postgresql://localhost:5432/mydb
# For Docker (connects to postgres service)
DB_URL_DOCKER=jdbc:postgresql://postgres:5432/mydb
DB_USERNAME=admin
DB_PASSWORD=admin
